<UserControl x:Class="CoffeeFlow.Nodes.EventNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="200">

    <Grid>
        <!-- Event Node UI Layout -->
        <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5" Background="LightGray">
            <Grid>
                <!-- Node Title -->
                <TextBlock Text="{Binding NodeName}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,5,0,0" />

                <!-- Input Connector -->
                <Ellipse x:Name="InExecutionConnector" Fill="Red" Width="10" Height="10" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0" />

                <!-- Output Connector -->
                <Ellipse x:Name="OutExecutionConnector" Fill="Green" Width="10" Height="10" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
